# Third-Party Dependencies

This folder contains third-party runtime assemblies required by the recovered launcher source.

## SharpDX

- `SharpDX.dll`
- `SharpDX.DirectInput.dll`

These assemblies are included so the recovered project can build reproducibly from a clean clone.

Review third-party licensing before publishing release binaries.
